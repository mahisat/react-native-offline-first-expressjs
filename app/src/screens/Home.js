import React from 'react';
import {
  SafeAreaView,
  View,
  FlatList,
  StyleSheet,
  Text,
  StatusBar,
  TouchableOpacity,
} from 'react-native';

import ListQuote from '../screens/ListQuotes';

const App = () => {
  return <ListQuote />;
};

export default App;
